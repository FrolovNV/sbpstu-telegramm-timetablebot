# Spbstu Timetable Bot

Open source telegram bot, which you can try at the link: @spbstu_timetable_bot in telegram.

# Install all library 

```bash
pip install pyTelegramBotAPI
pip install requests
pip install bs4
```
